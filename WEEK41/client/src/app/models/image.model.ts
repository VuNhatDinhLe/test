//import { ObjectId } from "mongoose";
import { Buffer } from 'buffer/';

export class Image {

    id?: any;
    name?: string;
    description?: string;
    
        data?: Buffer;
        contentType?: String;
    
  }
  